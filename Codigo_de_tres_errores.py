def suma_de_dos_numeros_enteros (x,y):
    if x == type(int) and y == type(int):
        print(x + y)        #Imprime el resultado de la suma
    else:
        print("Asegurese de que los numeros son enteros")

# Errores
# 1.En la primer linea los () de la función están separados del nombre.
# 2.Las variables (x,y), la y no lleva espacion después de la coma.
# 3.El comentario no tiene espacio después del "# aa".
